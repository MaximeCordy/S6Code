import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

export function initAutoportrait() {
  // Point d'entrée pour les animations de la page autoportrait
  // Ajouter ici les animations spécifiques à la page
}
